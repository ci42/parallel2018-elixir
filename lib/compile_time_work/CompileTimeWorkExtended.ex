# defmodule CompileTimeWorkExtended do
  
#   bif = function_exported?(:erlang, :is_map, 1)
  
#   if bif do
#     IO.puts "Use native built-in maps"
#   else
#     IO.puts "Use elixir implememtation"
#   end

# end
